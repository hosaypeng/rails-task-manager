My Tasks

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title.capitalize,  task_path(task)%></li>
  <%= link_to "Delete",
    task_path(task),
    data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
  %>
  <% end %>
</ul>

<%= link_to("Add New Task", tasks_new_path) %>
